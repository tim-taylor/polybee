% Custom LaTeX preamble injected into the PDF build only (see build.sh).
% Not applied to the docx build - docx styling is controlled via reference.docx.
%%% SARAH RAUCHAS' STUFF FOR PAGE LAYOUT
% documentclass options (11pt, twoside) are set via `classoption:` in
% report.md's YAML header instead - pandoc emits its own \documentclass
% line before this file is included, so one here would be a second,
% conflicting \documentclass command.
\usepackage{a4wide,charter}
\usepackage[T1]{fontenc}
\usepackage{amssymb}
\usepackage{framed}
\usepackage[normalem]{ulem}
\renewcommand{\labelitemi}{%
  \raisebox{1.3pt}{\footnotesize$\blacktriangleright$}}

\usepackage{fancyhdr}
\addtolength{\headheight}{1.0pt}
\pagestyle{fancy}
\fancyhead[RO,LE]{\thepage}
\fancyhead[LO,RE]{}
\fancyfoot{}
\renewcommand{\headheight}{14.5pt}

\setlength{\parskip}{1.1ex}
\setlength{\parindent}{0 pt}

\newcommand{\head}[1]{{\bf #1}}
\newcommand{\colwidth}{10.5 cm}
\newcommand{\colwidthtwo}{12.7 cm}
\newenvironment{coltab}{%
    \renewcommand{\arraystretch}{1}%
    \begin{tabular}[t]{@{}l@{}}%
  }{%
    \end{tabular}%
}
\newcommand{\colbox}[1]{\parbox[t]{\colwidth}{#1}}
\newcommand{\colboxtwo}[1]{\parbox[t]{\colwidthtwo}{#1}}
